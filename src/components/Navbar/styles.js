import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        backgroundColor: '#45B39D',
        flexDirection: 'row',
        height: 70,
        alignItems: 'center'
    },
    input: {
        borderWidth: 1,
        borderRadius: 10,
        borderColor: 'black',
        width: 200,
        height: 30,
        marginLeft: 10,
    }
})